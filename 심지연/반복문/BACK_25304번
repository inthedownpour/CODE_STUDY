#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main() {
	int X,N;
	int sum = 0;
	scanf("%d", &X);
	scanf("%d", &N);
	
	for (int i = 1; i <= N; i++) {
		int a = 1, b = 1;
		scanf("%d %d", &a, &b);
		sum += a * b;
	}
	if (sum == X) {
		printf("Yes");
	}
	else printf("No");
	return 0;
}
