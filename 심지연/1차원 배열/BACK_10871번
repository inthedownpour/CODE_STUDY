#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main() {
	int N = 1, X = 1;
	int a[10000];

	scanf("%d %d", &N, &X);

	for (int i = 0; i < N; i++) {
		scanf("%d", &a[i]);
	}

	for (int j = 0; j < N; j++) {
		if (X > a[j]) {
			printf("%d ", a[j]);
		}
	}

	return 0;
}
