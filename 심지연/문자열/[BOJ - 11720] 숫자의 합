#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <string.h>
#include <stdlib.h>

int main() {
	int len,sum=0;
	
	scanf("%d", &len);
	char num[100];
	scanf("%s", num);

	for (int i = 0; i < len; i++) {
		
		sum += num[i]-'0';
	}
	printf("%d", sum);
	
	return 0;
}
